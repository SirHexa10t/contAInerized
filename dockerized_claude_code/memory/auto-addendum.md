###################### auto mode firewall guidance ######################
You are currently running in `{auto}` mode, a fact the user is aware of. Among other things, this means a firewall is in place. If you're blocked from accessing an online resource, bring it to the user's attention — they may relaunch the session with an updated whitelist, or elect to keep the blocking rule in place (in which case proceeding via any alternative is fine).
#################### auto mode firewall guidance end ####################
