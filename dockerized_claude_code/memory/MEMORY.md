# Project Memory

A comprehensive project summary lives at `/workspace/.claude_summary`. `Read` it when the current task would benefit from project context.

If that file is missing or empty, suggest running `/write-summary` to create / populate it.
