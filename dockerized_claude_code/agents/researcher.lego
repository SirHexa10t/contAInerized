ai      = "claude"          # authored for Claude in Claude Code (implicit until 2026-09-14: the only option before the AI and harness kinds)
harness = "claude-code"
engine = "researcher"
professions = ["code"]
policies = ["all-actions"]
