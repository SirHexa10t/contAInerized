Upgrade the agents by updating their .conf files in /workspace/agents/ so they use the latest ANTHROPIC_MODEL.

- Keep the tier (haiku stays haiku, sonnet stays sonnet); only bump the version.
- "Latest" means the most recent version per the official Anthropic docs at docs.anthropic.com/en/docs/about-claude/models — check there first. If that page is unreachable or its contents are unclear, fall back to a general web lookup. If web lookup also fails, fall back to your training data and flag the version chosen as derived from training-data knowledge (potentially stale).
- Update active ANTHROPIC_MODEL assignments, plus any commented-out example references in the same files (so the documentation stays consistent with what you applied).
- If a conf has a comment pinning the version as deliberate, leave it untouched and report it.
- If changes are made, list each file with before → after.
- If everything is already current, say so and give approximate release timing where the model ID carries a date suffix or you know it confidently; acknowledge uncertainty otherwise.

Note on the agents folder: /workspace/agents/ is the directory containing the project's agent `.md` and `.conf` files. If that path is empty, missing, or doesn't contain those files, locate the correct directory (it's the one with both `.md` and matching `.conf` agent files); if you can't find it, ask the user where it lives before proceeding.
