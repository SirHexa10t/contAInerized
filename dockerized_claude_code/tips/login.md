# Login & account types

Make sure your OAuth login generates a key for the appropriate account type. **Credits are not shared between API, Pro, and other account types** — pick the wrong one and your balance won't be where you expect it (or you'll hit an unexpected wall).
